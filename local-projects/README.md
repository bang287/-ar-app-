# Local AR Projects

This folder is managed by the local demo API.

Each AR project is stored as:

- `project_<id>/project.json`
- `project_<id>/trigger/`
- `project_<id>/assets/`
- `project_<id>/thumbs/`

Later, this structure can map directly to Supabase Storage paths and database rows.
